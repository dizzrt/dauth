{
  "editor.formatOnSave": true,
  "protoc": {
    "options": [
      "--proto_path=${workspaceFolder}/api/proto",
      "--proto_path=${workspaceFolder}/third_party"
    ]
  },
  "gopls": {
    "build.buildFlags": ["-tags=wireinject"],
    "build.standaloneTags": ["ignore", "wireinject", "!wireinject"]
  }
}
